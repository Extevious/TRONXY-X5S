[gcode_macro SEARCH_VARS_TEST]
description: Searches for a variable by name.
gcode:
   {% set search = params.S|lower %}

   {% set ns = namespace() %}

   {% for item in printer  %}

      {% if ' ' in item %} {% set ns.path = ['printer', "['%s']" % (item), ''] %}
      {% else           %} {% set ns.path = ['printer.', item, '']             %}
      {% endif          %} 

       #{% if printer[item].items() %}

           #{% for key, child in printer[item].items() recursive %}

            #   {% set ns.path = ns.path[:loop.depth|int + 1] %}

               #{% if ' ' in key %} {ns.path.append("['%s']" % (key))}
               #{% else          %} {ns.path.append(".%s" % (key))}
               #{% endif %} 

            # {% if child is mapping %}

               # {loop(child.items())}

            # {% else %}

               # {% if search in ns.path|lower %}

                  # { action_respond_info("%s : %s" % (ns.path|join, child)) }  

               # {% endif %} 

            # {% endif %} 

          #{% endfor %}

       #{% endif %}

       
      {action_respond_info(ns.path)}

    {% endfor %}