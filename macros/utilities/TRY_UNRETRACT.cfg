[gcode_macro TRY_UNRETRACT]
description: Tries to unretract filament.
gcode:
   {% if printer.extruder.can_extrude %}

      G11
      
   {% else  %}

      RESPOND TYPE=error MSG="Failed to unretract filament."

   {% endif %}