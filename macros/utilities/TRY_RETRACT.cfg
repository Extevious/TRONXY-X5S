[include _RESPOND_COMMON.cfg]

[gcode_macro TRY_RETRACT]
description: Tries to retract filament.
gcode:
   # State validation
   {% if printer.extruder.can_extrude %}

      # Retract gcode
      G10

   {% endif %}